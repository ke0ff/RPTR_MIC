Hardware-only project to create a speaker/mic combination that is targeted at repeater port testing or as a user-interface for a VOIP terminal.

Includes GERBER/SCHEM/BOM (HW folder) and 3D source (3D folder) for ancilliary packaging.  3D is in OpenSCAD and includes snapshots of the current revision outputs in STL and PrusaSlicer formats.
